# URL base de la API del backend, con el prefijo de version incluido.
# Ejemplo: https://transfer-black-api.onrender.com/api/v1
VITE_API_URL=

# Opcional: template de tiles para el mapa de seguimiento publico (/track).
# Por defecto usa el basemap oscuro de CARTO.
VITE_MAP_TILES_URL=
